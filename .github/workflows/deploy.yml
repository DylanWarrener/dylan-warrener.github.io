name: Build & Deploy to Production

on:
  push:
    branches: [production]
    paths-ignore:
      - 'docs/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      NUXT_ENV: production
      NUXT_APP_BASE_URL: /

    steps:
      # 1) Checkout + keep the token so we can push back
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true

      # 2) Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      # 3) Install & QA
      - name: Install dependencies
        run: npm install --include=optional

      - name: Install Linux oxc parser binding
        run: npm install @oxc-parser/binding-linux-x64-gnu --no-save

      # 4) Generate into docs/ (Nuxt + Nitro)
      - name: Build static site into docs/
        run: npm run build:prod

      # 5) Flatten docs/public into docs/
      - name: Flatten docs/public into docs/
        shell: bash
        run: |
          shopt -s dotglob
          mv docs/public/* docs/
          rm -rf docs/public

      # 6) Prevent GitHub Pages from running Jekyll
      - name: Add nojekyll marker
        run: touch docs/.nojekyll

      # 7) Commit docs/ back to production
      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs
          git commit -m "chore: rebuild site at ${{ github.sha }}" || echo "Nothing to commit"
          git push origin production

import colors from "vuetify/util/colors"

const lightTheme = {
  default: "",
}
const darkTheme = {
  default: "",
}

const theme = {
  defaultTheme: "light",
  themes: {
    light: lightTheme,
    dark: darkTheme,
  },
}

export default theme
